services:
  web:
    image: nextjs-week10-app:local
    container_name: nextjs-week10-container
    network_mode: host
    environment:
      - NODE_ENV=production
    env_file:
      - .env.local
    volumes:
      - db_data:/app/prisma
    restart: unless-stopped

volumes:
  db_data:
